# CNAP AI SIEM Copilot — Open WebUI Additional Dependencies
# These are installed on top of the base Open WebUI image

# AWS SDK for IAM authentication in custom functions
boto3>=1.34.0,<2.0
botocore>=1.34.0,<2.0

# OpenSearch client for real-time log queries
opensearch-py>=2.4.0,<3.0
requests-aws4auth>=1.2.3,<2.0

# HTTP client
httpx>=0.26.0,<1.0
