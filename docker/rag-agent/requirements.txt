# CNAP AI SIEM Copilot — RAG Agent Python Dependencies
# Pin major versions for reproducible builds

# AWS SDK — IAM authentication, S3, boto3 credential chain
boto3>=1.34.0,<2.0
botocore>=1.34.0,<2.0

# OpenSearch client with AWS SigV4 auth support
opensearch-py>=2.4.0,<3.0
requests-aws4auth>=1.2.3,<2.0

# HTTP client for Ollama API
httpx>=0.26.0,<1.0

# YAML configuration parsing
PyYAML>=6.0.1,<7.0

# PDF text extraction (optional — for PDF knowledge base documents)
pypdf>=4.0.0,<5.0

# Type checking and data validation
pydantic>=2.5.0,<3.0
